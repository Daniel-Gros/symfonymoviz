{{ form_start(form) }}
    {{ form_widget(form) }}
    
    <button class="btn" onclick="resetForm()">Enregistrer</button>

{{ form_end(form)}}


<script>
    function resetForm() {
        var form = document.querySelector('form');
        form.reset();
    }
</script>